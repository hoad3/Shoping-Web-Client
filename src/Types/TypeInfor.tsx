export interface UserInfo {
    idname: number;
    user_id: number;
    username: string;
    phone: number;
    email: string;
    address: string;
    user: string; // Đổi tùy theo dữ liệu thực tế hoặc có thể để `null`
}